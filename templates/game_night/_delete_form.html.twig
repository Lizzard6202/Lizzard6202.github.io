<form method="post" action="{{ path('app_game_night_delete', {'id': game_night.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ game_night.id) }}">
    <button class="btn">Delete</button>
</form>
